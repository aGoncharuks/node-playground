module.exports = {
	port: '3001',
	host: '127.0.0.1',
	publicRootDir: `${process.cwd()}/public`,
	filesRootDir: `${process.cwd()}/files`,
	maxFileSize: 0.1e6,
  fixturesRootDir: `${process.cwd()}/test/fixtures`
};

